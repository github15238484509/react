import React from 'react'
// import { searchStudents } from "../services/student"

export default function index() {
  // searchStudents().then(data => console.log(data))
  return (
    <div>
      欢迎页
    </div>
  )
}
